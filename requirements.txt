# Core RAG Dependencies - FREE VERSION (no API keys needed!)
sentence-transformers>=2.0.0
chromadb>=0.4.0
python-dotenv>=1.0.0

# Document Processing
PyPDF2>=3.0.0
python-docx>=1.0.0

# Basic utilities
numpy>=1.21.0
requests>=2.25.0

# Optional: For advanced free models (uncomment if you want to try these)
# transformers>=4.20.0
# torch>=1.12.0
# ollama>=0.1.0
urllib3>=2.6.0 # not directly required, pinned by Snyk to avoid a vulnerability
